# PROJECT_PLAN

## Context Boundary / 上下文边界

`PROJECT_PLAN.md` 描述当前有效的未来执行计划。

本文件只记录计划如何推进；已经实际发生的项目状态变化应记录在 `PROJECT_STATE.md`。

## Execution Order / 总体执行顺序

<!--
必须使用 Markdown 有序列表表示当前有效计划中各 Phase 的总体推进顺序。

每个编号项只记录一个计划推进的 Phase。

编号顺序表示当前计划中 Phase 的总体执行顺序。

只列出当前有效计划实际包含的 Phase。

Agent 根据实际 Phase 数量增加或删除编号项。
-->

1. 待填写

## Phase Plan: 待填写

<!--
一个 Phase 使用一个独立的 Phase Plan Block。

标题必须填写该 Phase 的 Phase ID 和 Phase Name，例如：

## Phase Plan: P2 — Solution Design

如果当前计划包含多个 Phase，复制整个 Phase Plan Block。

不同 Phase 的 Goal、Task、Dependency、Expected Artifact、Risk 和 Validation Method 不得混写。
-->

### Phase Goal / 阶段目标

<!--
只填写当前 Phase 准备达成的目标。

一个 Goal 一个 Markdown 无序列表项。
-->

- 待填写

### Phase Task / 阶段任务

<!--
必须使用 Markdown 有序列表记录当前 Phase 中计划执行的 Task。

每个编号项只记录一个 Task。

编号顺序表示当前 Phase 内 Task 的计划执行顺序。

Agent 根据实际 Task 数量增加或删除编号项。
-->

1. 待填写

### Dependency / 依赖关系

<!--
只填写当前 Phase 或当前 Phase 中 Task 开始执行前需要满足的依赖。

一个 Dependency 一个 Markdown 无序列表项。

无依赖时填写：

- 无
-->

- 待填写

### Expected Artifact / 预期 Artifact

<!--
只填写当前 Phase 计划产生或更新的 Artifact。

一个 Artifact 一个 Markdown 无序列表项。
-->

- 待填写

### Risk / 风险

<!--
只填写当前 Phase 推进过程中当前已知的风险。

一个 Risk 一个 Markdown 无序列表项。

无已知风险时填写：

- 无
-->

- 待填写

### Validation Method / 验证方式

<!--
只填写用于确认当前 Phase 计划结果是否满足预期的 Validation Method。

一个 Validation Method 一个 Markdown 无序列表项。
-->

- 待填写
